apiVersion: v1
kind: Service
metadata:
  name: prime-clone
  namespace: prime-clone
spec:
  type: LoadBalancer          # provisions an AWS ELB -> public URL
  selector:
    app: prime-clone
  ports:
    - port: 80
      targetPort: 80
